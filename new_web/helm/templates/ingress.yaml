{{- if .Values.vue.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "vue-frontend.fullname" . }}
  labels:
    {{- include "vue-frontend.labels" . | nindent 4 }}
  {{- with .Values.vue.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.vue.ingress.className }}
  ingressClassName: {{ .Values.vue.ingress.className }}
  {{- end }}
  {{- if .Values.vue.ingress.tls }}
  tls:
    {{- range .Values.vue.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.vue.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "vue-frontend.fullname" $ }}
                port:
                  number: {{ $.Values.vue.service.port }}
          {{- end }}
    {{- end }}
{{- end }} 